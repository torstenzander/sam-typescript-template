{
  "name": "{{cookiecutter.project_name.lower().replace(' ', '-')}}",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.js",
  "author": "Torsten Zander",
  "license": "MIT",
  "scripts": {
    "lint": "node_modules/.bin/tslint -p tsconfig.json -c tslint.json",
    "clean": "",
    "test": "npm test \"mocha tests/**/*.spec.js\"s",
    "prebuild": "npm run lint && npm run clean",
    "build": "node_modules/.bin/webpack",
    "build-watch": "npm run prebuild && tsc -w --preserveWatchOutput true",
    "prestart": "npm run build",
    "start": "sam local start-api"
  },
  "dependencies": {
    "@types/aws-lambda": "*",
    "@types/node": "^10.12.26",
    "aws-lambda": "*",
    "aws-sdk": "*"
  },
  "devDependencies": {
    "@types/chai": "^4.1.7",
    "@types/mocha": "^5.2.6",
    "chai": "^4.1.2",
    "@types/node": "12.0.12",
    "ts-loader": "6.0.4",
    "tslint": "5.18.0",
    "typescript": "3.5.2",
    "webpack": "4.35.2",
    "webpack-cli": "3.3.5",
    "chakram": "^1.5.0"
  }
}
